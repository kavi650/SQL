DECLARE
  TYPE names IS VARRAY(3) OF VARCHAR2(10);
  TYPE marks IS VARRAY(3) OF NUMBER;

  n names := names('Kavita','Pritam','Ayan');
  m marks := marks(98,97,78);

BEGIN
  FOR i IN 1..n.COUNT LOOP
    DBMS_OUTPUT.PUT_LINE(n(i) || ' : ' || m(i));
  END LOOP;
END;
/
